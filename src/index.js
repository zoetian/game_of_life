const boardSize = 1000;
const rows = 60;
const pictonBlue = "#40C4FF";

const getRandomGrid() = () => {
    const grid = new Array(rows)
    for (let i = 0; i < grid.length; i++) {
        grid[i] = new new Array(row)
            for(let j = 0; j < )
    }
}

window.onload = () => {
    const canvas = document.getElementById('canvas');
    const ctx = canvas.getContext('2d';)
}
